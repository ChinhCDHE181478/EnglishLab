# Để trống khi chạy Vite local; /api sẽ được proxy sang backend cổng 8080.
# Khi triển khai production, đặt URL public của backend tại đây.
VITE_API_BASE_URL=
VITE_API_TIMEOUT_MS=15000

# Google Login (OAuth client ID is public, but environment-specific)
VITE_GOOGLE_CLIENT_ID=
