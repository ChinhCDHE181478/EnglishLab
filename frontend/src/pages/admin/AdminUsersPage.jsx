import { useCallback, useEffect, useState } from 'react';
import { Edit3, Plus, Search, UserCog } from 'lucide-react';
import adminApi from '../../api/adminApi';
import { ManagerFilterBar, ManagerStatusBadge, ManagerTable } from '../../components/content-manager/ManagerListUi';
import BrandedSelect from '../../components/ui/BrandedSelect';

const ROLES = ['LEARNER', 'TEACHER', 'CONTENT_MANAGER', 'TRAINING_MANAGER', 'MANAGER', 'ADMIN'];
const emptyForm = { fullName: '', email: '', phoneNumber: '', password: '', roles: ['LEARNER'] };

export default function AdminUsersPage() {
  const [users, setUsers] = useState([]); const [keyword, setKeyword] = useState(''); const [role, setRole] = useState('');
  const [page, setPage] = useState(0); const [totalPages, setTotalPages] = useState(0); const [modal, setModal] = useState(null);
  const [form, setForm] = useState(emptyForm); const [error, setError] = useState('');
  const load = useCallback(async () => { setError(''); try { const data = await adminApi.getUsers({ keyword: keyword || undefined, role: role || undefined, page, size: 15, sort: 'createdAt,desc' }); setUsers(data.content || []); setTotalPages(data.totalPages || 0); } catch (err) { setError(err.response?.data?.message || 'Không tải được người dùng.'); } }, [keyword, role, page]);
  useEffect(() => { load(); }, [load]);
  const openCreate = () => { setModal('create'); setForm(emptyForm); };
  const openEdit = (user) => { setModal(user); setForm({ fullName: user.fullName || '', email: user.email || '', phoneNumber: user.phoneNumber || '', password: '', roles: user.roles || [] }); };
  const toggleRole = (value) => setForm((current) => ({ ...current, roles: current.roles.includes(value) ? current.roles.filter((item) => item !== value) : [...current.roles, value] }));
  const save = async (event) => { event.preventDefault(); setError(''); try { if (modal === 'create') await adminApi.createUser(form); else await adminApi.updateUser(modal.id, form); setModal(null); await load(); } catch (err) { setError(err.response?.data?.message || 'Không lưu được người dùng.'); } };
  const toggleStatus = async (user) => { try { await adminApi.updateUserStatus(user.id, !user.enabled); await load(); } catch (err) { setError(err.response?.data?.message || 'Không đổi được trạng thái.'); } };
  return <div className="space-y-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#8a0018]">Phân quyền</p><h1 className="mt-2 font-['Manrope'] text-3xl font-extrabold tracking-tight text-slate-900">Người dùng & vai trò</h1><p className="mt-2 text-sm text-slate-500">Quản lý trạng thái tài khoản và vai trò trong hệ thống.</p></div><button className="inline-flex items-center justify-center gap-2 rounded-lg bg-[#4b0009] px-5 py-3 text-sm font-bold text-white shadow-sm hover:bg-[#730014]" onClick={openCreate} type="button"><Plus className="h-4 w-4" />Tạo người dùng</button></div><ManagerFilterBar><label className="flex min-w-[260px] flex-1 items-center gap-2 rounded-lg border border-[#dcc0bf]/40 bg-[#fffdfc] px-3"><Search className="h-4 w-4 text-slate-400" /><input className="w-full bg-transparent py-2.5 text-sm outline-none" onChange={(event) => { setKeyword(event.target.value); setPage(0); }} placeholder="Tìm tên, email, số điện thoại" value={keyword} /></label><BrandedSelect buttonClassName="min-w-[210px] rounded-lg border-[#dcc0bf]/40 py-2.5 text-sm shadow-none" onChange={(event) => { setRole(event.target.value); setPage(0); }} options={[{ label: 'Tất cả vai trò', value: '' }, ...ROLES.map((item) => ({ label: item, value: item }))]} value={role} /></ManagerFilterBar>{error ? <p className="rounded-2xl border border-rose-200 bg-rose-50 p-4 text-sm font-semibold text-rose-700">{error}</p> : null}<section className="overflow-hidden rounded-xl border border-[#dcc0bf]/30 bg-white shadow-sm"><ManagerTable columns={[{ label: 'Người dùng' }, { label: 'Vai trò' }, { label: 'Hồ sơ' }, { label: 'Trạng thái' }, { label: 'Thao tác', align: 'right' }]} minWidth="1000px">{users.map((user) => <tr className="hover:bg-[#eff4ff]/35" key={user.id}><td className="px-6 py-5"><p className="font-bold text-[#0b1c30]">{user.fullName}</p><p className="mt-1 text-xs text-[#756361]">{user.email}{user.phoneNumber ? ` · ${user.phoneNumber}` : ''}</p></td><td className="px-6 py-5"><div className="flex flex-wrap gap-1">{user.roles.map((item) => <ManagerStatusBadge key={item} tone="info">{item}</ManagerStatusBadge>)}</div></td><td className="px-6 py-5 text-sm text-[#564241]">{user.profileCompleted ? 'Đã hoàn thiện' : 'Chưa hoàn thiện'}</td><td className="px-6 py-5"><button className={`rounded-full px-3 py-1 text-xs font-bold ${user.enabled ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-200 text-slate-600'}`} onClick={() => toggleStatus(user)} type="button">{user.enabled ? 'Hoạt động' : 'Đã khóa'}</button></td><td className="px-6 py-5 text-right"><button className="inline-flex items-center gap-2 rounded-lg border border-[#dcc0bf]/50 px-3 py-2 text-xs font-bold text-[#730014] hover:bg-[#fff6f6]" onClick={() => openEdit(user)} type="button"><Edit3 className="h-4 w-4" />Sửa / vai trò</button></td></tr>)}</ManagerTable><div className="flex items-center justify-end gap-3 border-t border-[#dcc0bf]/20 bg-[#eff4ff]/30 px-6 py-4"><button className="rounded-lg border border-[#dcc0bf]/40 px-3 py-2 text-sm disabled:opacity-40" disabled={page <= 0} onClick={() => setPage((value) => value - 1)} type="button">Trang trước</button><span className="text-sm font-semibold text-[#564241]">Trang {page + 1} / {Math.max(1, totalPages)}</span><button className="rounded-lg border border-[#dcc0bf]/40 px-3 py-2 text-sm disabled:opacity-40" disabled={page + 1 >= totalPages} onClick={() => setPage((value) => value + 1)} type="button">Trang sau</button></div></section>{modal ? <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#1a0004]/45 p-4"><form className="w-full max-w-2xl rounded-3xl border border-[#ead9db] bg-white p-6 shadow-2xl" onSubmit={save}><div className="mb-5 flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-2xl bg-[#fff1f2] text-[#730014]"><UserCog className="h-5 w-5" /></span><h2 className="font-['Manrope'] text-2xl font-extrabold text-[#4b0009]">{modal === 'create' ? 'Tạo người dùng' : 'Chỉnh sửa người dùng'}</h2></div><div className="grid gap-4 sm:grid-cols-2">{[['fullName', 'Họ tên', 'text'], ['email', 'Email', 'email'], ['phoneNumber', 'Số điện thoại', 'text'], ['password', modal === 'create' ? 'Mật khẩu' : 'Mật khẩu mới (tùy chọn)', 'password']].map(([key, placeholder, type]) => <input className="rounded-xl border border-[#dcc0bf]/50 p-3 outline-none focus:border-[#8a0018]" key={key} placeholder={placeholder} required={key === 'fullName' || key === 'email' || (key === 'password' && modal === 'create')} type={type} value={form[key]} onChange={(event) => setForm({ ...form, [key]: event.target.value })} />)}</div><p className="mb-2 mt-5 text-sm font-bold text-[#4b0009]">Vai trò</p><div className="flex flex-wrap gap-2">{ROLES.map((item) => <label className={`cursor-pointer rounded-full border px-3 py-2 text-xs font-bold ${form.roles.includes(item) ? 'border-[#730014] bg-[#fff1f2] text-[#730014]' : 'border-slate-200 text-[#564241]'}`} key={item}><input checked={form.roles.includes(item)} className="sr-only" onChange={() => toggleRole(item)} type="checkbox" />{item}</label>)}</div><div className="mt-6 flex justify-end gap-3"><button className="rounded-xl border border-[#dfbfbd] px-5 py-2.5 font-bold text-[#730014]" onClick={() => setModal(null)} type="button">Hủy</button><button className="rounded-xl bg-[#4b0009] px-5 py-2.5 font-bold text-white disabled:opacity-50" disabled={!form.roles.length} type="submit">Lưu</button></div></form></div> : null}</div>;
}
